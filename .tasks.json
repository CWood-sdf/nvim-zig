{
  "tasks": [
    {
      "name": "run",
      "cmd": "cd nvim-zig && zig build run && nvim -c 'lua vim.defer_fn(function() vim.cmd(\"so run.lua\") end, 200)'",
      "window": {
        "name": "zook",
        "close": true
      }
    },
    {
      "name": "debug",
      "cmd": "cd nvim-zig && zig build run && nvim -c 'lua vim.defer_fn(function() vim.cmd(\"so run.lua\") end, 200)'",
      "window": {
        "name": "zookd",
        "close": false
      }
    }
  ]
}
